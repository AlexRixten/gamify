import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  badgeWrapper: {
    alignItems: 'center',
    marginBottom: 16,
  },
  label: {
    fontSize: 11,
    color: '#666',
    marginTop: 4,
    textAlign: 'center',
    maxWidth: 80,
  },
});
